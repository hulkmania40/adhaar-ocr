[project]
name = "adhaar-ocr"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "python-dotenv",
    "google-genai",
    "Pillow"
]